{% extends 'base.html.twig' %}

{% block title %}RendezVous index{% endblock %}

{% block body %}
    <h1>RendezVous confirmation</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Date</th>
            <th>Heure</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for rendez_vous in rendez_vous %}
            <tr>
                <td>{{ rendez_vous.id }}</td>
                <td>{{ rendez_vous.date ? rendez_vou.date|date('Y-m-d') : '' }}</td>
                <td>{{ rendez_vous.heure }}</td>
                <td>
                    <a href="{{ path('app_rendez_vous_show', {'id': rendez_vou.id}) }}">show</a>
                    <a href="{{ path('app_rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_new') }}">Create new</a>
{% endblock %}
