{% extends 'base.html.twig' %}

{% block title %}Amphitheatre{% endblock %}

{% block body %}
    <h1>Amphitheatre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amphitheatre.id }}</td>
            </tr>
            <tr>
                <th>Place_libre</th>
                <td>{{ amphitheatre.placeLibre }}</td>
            </tr>
        </tbody>
    </table>
<button type="button" class="btn btn-outline-info">
    <a href="{{ path('app_amphitheatre_index') }}">Retour à la lite</a>
</button>
    <button type="button" class="btn btn-outline-info">
    <a href="{{ path('app_amphitheatre_edit', {'id': amphitheatre.id}) }}">Editer</a>
    </button>

    {{ include('amphitheatre/_delete_form.html.twig') }}
{% endblock %}
