<form method="post" action="{{ path('app_amphitheatre_delete', {'id': amphitheatre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amphitheatre.id) }}">
    <button class="btn" class="btn btn-outline-info">Delete</button>
</form>
