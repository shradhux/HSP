{% extends 'base.html.twig' %}

{% block title %}Postuler{% endblock %}

{% block body %}
    <h1>Postuler</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postuler.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_postuler_index') }}">back to list</a>

    <a href="{{ path('app_postuler_edit', {'id': postuler.id}) }}">edit</a>

    {{ include('postuler/_delete_form.html.twig') }}
{% endblock %}
